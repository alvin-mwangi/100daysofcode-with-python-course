
NAMES = ['arnold schwarzenegger', 'alec baldwin', 'bob belderbos',
         'julian sequeira', 'sandra bullock', 'keanu reeves',
         'julbob pybites', 'bob belderbos', 'julian sequeira',
         'al pacino', 'brad pitt', 'matt damon', 'brad pitt']

# Convert names to title case (brad pitt -> Brad Pitt)
# OR reverse the first and last name

# convert names to title case
# titleCaseNames = [name.title() for name in NAMES]
# print(titleCaseNames)

# reverse first and last name
# reversedNames = [' '.join(list(reversed(name.title().split()))) for name in NAMES]
# print(reversedNames)


